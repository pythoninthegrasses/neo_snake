# neo_snake

What started as a simple smoke test for local LLMs morphed into something incredible...

🕶️ 🐍

## Development

Copy `.env.example` to `.env`, then run:

```
task check
```

This is the only command documented here on purpose -- it is the one build/test entry point for this repo.

