---
id: TASK-004
title: Vendor godot-cpp at a pinned SHA
status: Done
assignee:
  - '@claude'
created_date: '2026-09-09 22:08'
updated_date: '2026-09-09 22:55'
labels: []
milestone: m-0
dependencies:
  - TASK-002
references:
  - 'https://github.com/godotengine/godot-cpp'
priority: high
type: spike
ordinal: 4000
---

## Description

<!-- SECTION:DESCRIPTION:BEGIN -->
Add third_party/godot-cpp as a git submodule. Verify at execution time whether a godot-4.7-stable tag has appeared upstream since this plan was written; pin that tag if so, otherwise pin a master commit SHA (4.7 support currently lives only on master — the latest tag is godot-4.5-stable). Note that gdextension_interface.h is now generated by make_interface_header.py, not vendored, so no header copy step is needed.
<!-- SECTION:DESCRIPTION:END -->

## Acceptance Criteria
<!-- AC:BEGIN -->
- [x] #1 git submodule status shows the pinned tag or commit SHA for third_party/godot-cpp
- [x] #2 scons -C third_party/godot-cpp api_version=4.7 builds successfully
- [x] #3 The pin choice (tag vs SHA) and rationale are recorded in backlog/decisions/
<!-- AC:END -->

## Definition of Done
<!-- DOD:BEGIN -->
- [ ] #1 task check is green
- [x] #2 Any deviation from reference/snake.html behavior is recorded in backlog/decisions/, not left implicit
- [x] #3 Docs touched by the change are updated in the same commit
- [x] #4 The task file's AC/notes/status are synced in the same commit as the code
<!-- DOD:END -->

## Implementation Plan

<!-- SECTION:PLAN:BEGIN -->
1. Re-verified at execution time (git ls-remote): no godot-4.7-stable tag exists upstream (latest tag is godot-4.5-stable); master HEAD is 6cceaf6a5f8b0d78ac5d71c139fd7fabba43b918 (same SHA already seeded into tools/game_toolchain.lock's GODOT_CPP_COMMIT by TASK-003). Pin third_party/godot-cpp to that exact commit SHA (not a floating branch).
2. `git submodule add https://github.com/godotengine/godot-cpp third_party/godot-cpp`, then `git -C third_party/godot-cpp checkout 6cceaf6a5f8b0d78ac5d71c139fd7fabba43b918` (detached HEAD pin), then `git add .gitmodules third_party/godot-cpp` to record the pinned gitlink.
3. Per the task note, gdextension_interface.h is generated by make_interface_header.py now (not vendored) -- no header copy step needed. Confirm this file/script exists in the checked-out submodule before relying on it.
4. Build: `scons -C third_party/godot-cpp api_version=4.7` (using mise's pipx:scons 4.11.1) and confirm it succeeds. godot-cpp needs its own bundled godot-headers/extension_api.json or will generate bindings from the installed Godot's --dump-extension-api-with-docs; check godot-cpp's SConstruct/README for what api_version=4.7 actually requires as input and supply it if not automatic.
5. Record the pin choice (SHA vs tag) and rationale in backlog/decisions/decision-001 - ... .md, following azure-dreams' frontmatter convention (id/title/date/status, Context/Decision/Consequences sections) since neo_snake's backlog/decisions/ is currently empty and no MCP tool manages this directory -- it's a plain file convention.
6. `git submodule status` should show the pinned SHA for third_party/godot-cpp (AC#1). No reference/snake.html behavior change from vendoring a build dependency -- DoD#2 is satisfied by the decisions entry itself being the AC#3/DoD#2 overlap (vendoring a third-party build tool at a SHA pin is exactly the kind of intentional deviation/decision this project's DoD#2 asks to have recorded, even though it's not a snake.html behavior change per se -- will note this explicitly rather than silently skip DoD#2).
7. docs/architecture.md is entirely about reference/snake.html and doesn't mention build dependencies -- no doc update needed unless a new docs file about the build pipeline is warranted (out of scope here; that's docs/abi-decisions.md territory per TASK-009). task check doesn't exist yet (TASK-006) -- DoD#1 N/A-until-then, same precedent as prior tasks.
<!-- SECTION:PLAN:END -->

## Implementation Notes

<!-- SECTION:NOTES:BEGIN -->
Executed the plan as recorded: submodule added and pinned to 6cceaf6a5f8b0d78ac5d71c139fd7fabba43b918 (detached HEAD), matching tools/game_toolchain.lock's GODOT_CPP_COMMIT seeded by TASK-003. `git submodule status` confirms: ` 6cceaf6a5f8b0d78ac5d71c139fd7fabba43b918 third_party/godot-cpp (godot-3.3.3-stable-1435-g6cceaf6)` -- AC#1 satisfied.

gdextension_interface.h confirmed generated (make_interface_header.py present at submodule top level, no vendored header to copy) -- matches the task description's note.

Ran `mise exec -- scons -C third_party/godot-cpp api_version=4.7`. Build completed successfully: `Linking Static Library bin/libgodot-cpp.linux.template_debug.x86_64.a ... scons: done building targets.` No custom_api_file was needed -- godot-cpp's gdextension/ directory ships bundled extension_api-4-7.json alongside 4-3/4-4/4-5/4-6, so api_version=4.7 resolves against that bundled definition automatically. AC#2 satisfied.

Wrote backlog/decisions/decision-001 - Pin-godot-cpp-to-a-master-commit-SHA-not-a-tag.md recording the SHA-vs-tag rationale (no godot-4.7-stable tag exists upstream; 4.7 bindings live only on master) and the re-verify-live-at-each-bump consequence. AC#3 satisfied.

DoD#2: this is a build-toolchain pin, not a reference/snake.html behavior deviation -- noted explicitly in the decision doc's Context section rather than silently skipped, per the plan's step 6 reasoning.

DoD#1 (task check) is N/A until TASK-006 introduces the Taskfile -- same precedent as TASK-003.

DoD#3: no docs/*.md update needed -- docs/architecture.md is scoped to reference/snake.html internals; build-pipeline docs are TASK-009's scope.
<!-- SECTION:NOTES:END -->

## Final Summary

<!-- SECTION:FINAL_SUMMARY:BEGIN -->
Vendored third_party/godot-cpp as a git submodule pinned to master commit 6cceaf6a5f8b0d78ac5d71c139fd7fabba43b918 (detached HEAD) -- no godot-4.7-stable tag exists upstream yet, so a SHA pin was used instead of a floating tag/branch, matching the value already seeded into tools/game_toolchain.lock by TASK-003. Verified the submodule builds cleanly against Godot 4.7 bindings via `scons -C third_party/godot-cpp api_version=4.7` (godot-cpp bundles extension_api-4-7.json, so no custom_api_file dump from the installed Godot was required). Recorded the SHA-vs-tag decision and its rationale in backlog/decisions/decision-001, seeding this repo's decision-log convention (borrowed from azure-dreams-remake) for the first time.
<!-- SECTION:FINAL_SUMMARY:END -->
